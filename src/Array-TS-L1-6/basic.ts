const favoritePeople = ['Ana', 'Bella', 'Carin', 'Diana'];

console.log(favoritePeople);

const firstPerson = favoritePeople.shift();

console.log('Entfernter Wert:', firstPerson);
console.log(favoritePeople);

const secondPerson = favoritePeople.shift();

console.log('Entfernter Wert:', secondPerson);
console.log(favoritePeople);